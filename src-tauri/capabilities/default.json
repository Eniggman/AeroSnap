{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "AeroSnap desktop windows",
  "windows": ["overlay", "settings", "recording-controls"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "autostart:default",
    "global-shortcut:default"
  ]
}
